import * as React from 'react';

interface IAddNewTestToCatalogProps {
}

export const AddNewTestToCatalog: React.FC<IAddNewTestToCatalogProps> = (props) => {
  return (
      <h1>add new test to catalog</h1>
  );
};

